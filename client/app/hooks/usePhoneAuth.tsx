const usePhoneAuth = () => {

    const handleSendCode = () => { }
    const handleVerifyCode = () => { }

    return {
        handleSendCode,
        handleVerifyCode
    };
};

export default usePhoneAuth;
